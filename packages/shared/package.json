{
  "name": "@cs-tipitaka/shared",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write . --config ../../.prettierrc --ignore-path ../../.prettierignore",
    "fix-all": "pnpm run lint:fix && pnpm run format"
  },
  "devDependencies": {
    "@types/node": "^24.1.0"
  }
}
