---
interface Props {
  data: Record<string, unknown>;
}
const { data } = Astro.props;
---

<!-- Escapes "<" — docs/web/scripts-and-transliteration.md -->
<script
  is:inline
  type="application/ld+json"
  set:html={JSON.stringify(data).replace(/</g, '\\u003c')}
/>
