{
  "name": "cs-tipitaka",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.17.0",
  "engines": {
    "node": ">=20.9.0 <26"
  },
  "scripts": {
    "dev": "pnpm run dev:web",
    "dev:web": "pnpm --filter @cs-tipitaka/web --fail-if-no-match run dev",
    "dev:api": "pnpm --filter @cs-tipitaka/api --fail-if-no-match run dev",
    "build": "pnpm run build:web",
    "build:web": "pnpm --filter @cs-tipitaka/web --fail-if-no-match run build",
    "build:api": "pnpm --filter @cs-tipitaka/api --fail-if-no-match run build",
    "lint": "pnpm -r run lint",
    "lint:fix": "pnpm -r run lint:fix",
    "format": "pnpm -r run format && pnpm run format:root",
    "format:root": "prettier --write --config ./.prettierrc --ignore-unknown \".prettierrc\" \"*.{cjs,css,html,js,json,jsonc,md,mjs,ts,yaml,yml}\"",
    "format:check": "prettier --check --config ./.prettierrc --ignore-unknown \".prettierrc\" \"*.{cjs,css,html,js,json,jsonc,md,mjs,ts,yaml,yml}\"",
    "fix-all": "pnpm -r run fix-all && pnpm run format:root",
    "preview": "pnpm --filter @cs-tipitaka/web --fail-if-no-match run preview",
    "deploy": "pnpm --filter @cs-tipitaka/web --fail-if-no-match run deploy",
    "file-length": "./file_length.sh"
  },
  "devDependencies": {
    "eslint": "^9.39.4",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.6",
    "prettier": "^3.9.6",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.62.0"
  }
}
